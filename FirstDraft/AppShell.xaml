<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FirstDraft.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FirstDraft"
    xmlns:view="clr-namespace:FirstDraft.View"
    xmlns:viewModel="clr-namespace:FirstDraft.ViewModel">


    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <!--<FlyoutItem Title="Database">
        <ShellContent Title="Festivals" ContentTemplate="{DataTemplate view:AllFestivalsPage}"/>
        <ShellContent Title="Bin" ContentTemplate="{DataTemplate view:BinPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="Extras">
        <ShellContent Title="About" ContentTemplate="{DataTemplate view:About}"/>
    </FlyoutItem>-->

</Shell>
